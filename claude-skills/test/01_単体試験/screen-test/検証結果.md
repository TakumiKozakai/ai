# 構築時の検証結果

実施日: 2026-09-21

| 確認 | 結果 |
| --- | --- |
| Python起動 | Python 3.14.7 |
| `python3 -m unittest discover -s tests -v` | 10件すべて成功。DB/AWS/ブラウザ呼び出しをモックしたローカル検証 |
| `python3 runner.py --config config.todo-local.json --validate` | 成功。外部接続なし |
| `git diff --check` | 成功 |
| Node/npm | ローカル導入のNode 22.23.2 / npm 10.9.8で起動成功。既存Node 16は変更なし |
| スキル `quick_validate.py` | PyYAML未導入により起動不可。必須frontmatterと参照先は内容確認済み |
| コード作成・実施スキルの形式 | 既存RubyのYAMLで両スキルのname・description・命名・ディレクトリ一致を検証し成功。追加依存なし |
| 新しい手順書・スキルのリンク | ローカル18リンクが実在することを確認 |
| 票からコードへの受け渡し例 | plans/todoの設定検証、既存specの構文、全体列挙とcase単位列挙が成功。元票No.1と1件のtestが一致 |
| npm依存・Chromium導入 | Playwright 1.55.1 / Chromium 140.0.7339.186導入済み。package-lock.json生成済み |
| Playwrightテスト列挙 | 成功。Todoの試験1件を検出 |
| 実ブラウザのオフライン動作確認 | 2件成功。入力・クリック・操作後PNG3枚・操作失敗時PNG1枚 |
| 実Todoアプリの画面打鍵 | 未実施。稼働中アプリ・DB設定・実接続許可が必要 |
| psql | PATH・一般的なHomebrew配置先に見当たらず。ユーザー指示により追加導入は当面実施しない |
| AWS CLI | 既存のaws-cli/2.4.7が起動成功 |
| DB実接続 | ユーザー指示により疎通確認は当面実施しない。準備手順を文書化 |
| S3・EC2実接続 | ユーザー指示により疎通確認は当面実施しない。準備手順を文書化 |

SSM経由のEC2確認を実装している。別方式を選ぶ場合は接続方法を変更する。
Playwright CLI・SSM API・S3ページングの公式仕様を確認済み。参照先は構築手順書に記載。
ローカル検証の成功は実環境の疎通確認成功を意味しない。

## ブラウザ動作確認の証跡

- 成功時: `runs/setup-smoke-20260921-02/playwright-report/index.html`
- 結果JSON: `runs/setup-smoke-20260921-02/playwright.json`
- PNG: `runs/setup-smoke-20260921-02/browser/` 以下の `step-*.png` 計4枚
- 最初のサンドボックス内実行はChromiumがSIGTRAP終了。
  その結果は `runs/setup-smoke-20260921-01/` に保持し、許可後のサンドボックス外実行で2件成功した。

端末はmacOS 12.7.6 / Intelで、Playwrightの文書上の対応OS範囲外。
今回の動作確認結果と、対応OS上での正式な運用確認は区別する。
Node配布アーカイブは公式のSHA-256公開値と一致することを確認して展開した。

## 確認済みの挙動

- 接続先origin・SELECT・設定不足の検出
- DB事前失敗時は画面操作を止め、DB事後確認は試みる
- 画面失敗時もDB事後・S3・EC2の確認を継続する
- DB差分やS3期待キー欠落を失敗として記録する
- psqlに読み取り専用トランザクションとタイムアウトを指定する
- SSMへの登録反映待ち、ログパスのシェル引用
- SSM出力が上限に達した場合に成功扱いしない
- Playwrightのskipを成功扱いしない
- HTMLにエラー文字列をエスケープして出力する

## TypeScript移行・seed/複数クエリ/appLog追加時の検証（2026-09-22）

| 確認 | 結果 |
| --- | --- |
| `npm run typecheck`（tsc 5.9 / strict / erasableSyntaxOnly） | 成功。型エラー0 |
| `npm run test:unit`（node:test） | 31件すべて成功。旧Python単体テスト10件の観点を移植し、seed・複数クエリ・appLogを追加。DB/AWS/ブラウザはモック |
| `node src/runner.ts --validate`（ecsite-sample・todo・config.todo-local） | 3設定とも成功。外部接続なし |
| `npm run test:list` | ecsite-sample 1件・todo 1件を列挙 |
| `npm run test:smoke` | 2件成功。TS版 evidence でPNG保存・失敗時PNG保存を確認 |
| psql・playwright_user・実DB/アプリへの接続 | 未実施。psql 導入後に ecsite-sample で確認する |
