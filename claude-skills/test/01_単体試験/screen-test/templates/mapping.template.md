# テストコード対応表: {画面名} / {分類}

- suite-id: `{suite-id}`
- 元票: `{screen-test/からの相対パス}`
- 元票SHA-256: `{生ファイルのSHA-256}`
- 根拠資料: `{設計書・ソース・ユーザーの確認済み回答。接続情報や秘密値は書かない}`
- 対象No.: `{全件、または明示指定されたNo.}`
- spec: `specs/{suite-id}.spec.js`
- 設定例: `plans/{suite-id}/config.example.json`
- ローカル実行設定: `plans/{suite-id}/config.local.json`（Git管理外）
- 全体状態: `{実施準備完了 / 実施不可}`

「実施準備完了」はコード・設定・初期状態の確認事項が解決済みの意味。試験に合格した意味ではない。

## No.との対応

| No. | シナリオ / test名 | 状態 | 操作→step / 画面assertion | DB前後の確認 | S3 | EC2 | 保留・対象外の理由 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| {No.} | {test名は設定titleと一致} | {実装済み / 保留 / 対象外} | {手順番号・assertion} | {SQLの根拠・期待値} | {期待キーまたは対象外理由} | {期待ログまたは対象外理由} | {理由} |

## 初期状態・認証・再実行条件

- 対象環境: {試験専用環境の識別。秘密値は記載しない}
- 認証: {不要、または認証方式と安全な設定方法}
- 初期データ: {ケースごとに必要な状態と用意する担当・手順}
- ケース間依存: {前のNo.の永続データへの依存。ブラウザ・メモリは共有されない}
- 再実行: {同じ状態に戻す別途承認済み手順、または非更新のため不要}
- 変更・削除を行う操作: {該当No.と対象データ、またはなし}

## 未確定事項

| 対象No. / 設定 | 確認事項 | 実施を止める理由 | 回答 |
| --- | --- | --- | --- |
| {対象} | {質問} | {不足する根拠} | {未回答 / 確認済み内容} |

未確定事項がない場合は「なし」と記載する。未使用の雛形行は成果物に残さない。

## 静的検証

| 検証 | 結果 | 補足 |
| --- | --- | --- |
| JavaScript構文 | {成功 / 失敗 / 未検証} | {実行したコマンド・理由} |
| Playwright列挙とNo.の一致 | {成功 / 失敗 / 未検証} | {期待件数と列挙件数} |
| 設定検証 | {成功 / 失敗 / 未検証} | {未設定項目があれば明記} |

## 実施前レビュー

- 対象No.の操作・期待結果・外部確認が票と一致: {未確認 / 確認済み}
- 準備データ・接続先・実行範囲: {未確認 / 確認済み}
- 構築時点の実施状況: 未実施

試験結果は実行時の `runs/<run-id>/` に保存し、この対応表の実装状態と混同しない。
