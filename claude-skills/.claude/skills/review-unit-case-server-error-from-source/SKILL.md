---
name: review-unit-case-server-error-from-source
description: 対象画面のソースコード（コントローラ・サービス・リポジトリ・例外ハンドラ等）とサーバエラー（異常系）の試験項目票（試験項目票_{画面名称}_03_異常系.md）を突き合わせ、DBエラー・ファイル操作エラー・外部サービスアクセスエラー等の観点での抜け・誤り・分類誤りをレビューする。「サーバエラー系の試験項目票をソースでレビューして」等の依頼、または review-unit-case-for-screen-from-source / finalize-unit-case-server-error から呼び出されて使用する。
context: fork
agent: unit-case-reviewer
background: false
---

引数 `$ARGUMENTS` で渡された「対象画面のソースファイルパス（複数可）」と「試験項目票ディレクトリ」をもとに、以下の手順でサーバエラー（異常系）の試験項目票をレビューしてください。

1. `$ARGUMENTS` からソースファイルパス・試験項目票ディレクトリ・画面名称（大元skillから渡された場合）を読み取る
   - ソースファイルパスまたは試験項目票ディレクトリが省略されている場合は、独断で進めず実行状態「失敗」と不足している引数を返す。相対パスの場合はカレントディレクトリ基準で解釈する
   - 「集約実行」の明示が無い場合は単体実行として振る舞う（違いはHTMLを作成するかどうかだけ）
   - 本skillは `context: fork` でサブエージェントとして動くため、ユーザーに質問するツールは使えない。単体実行・集約実行を問わずユーザーに確認せず、確認したい内容は未確認観点に添えて返す（呼び出し元がユーザーに確認し、「確認済み回答」を添えて再度呼び出す）。確認済み回答が渡された事項は再確認せずその回答を用いる。確認済み回答が照合元の記載と矛盾する場合は確認済み回答を優先し、その旨を指摘の根拠に明記する
   - 「未解決事項」（呼び出し元が作成時に判断できなかった事項）が渡された場合は、照合元で解消できるか確認し、解消できないものは確認したい内容を添えて未確認観点に含めて返す
2. ソースファイルと関連ソース（コントローラ・サービス・リポジトリ・外部サービスクライアント・例外ハンドラ（`@ExceptionHandler`／`@ControllerAdvice` 等）・エラーページテンプレート・メッセージ定義等）を読み込み、対象画面のサーバ処理が依存する外部リソース（DB・ファイル・AWSサービス等）へのアクセス処理と、例外発生時の挙動（捕捉箇所・表示されるエラーメッセージ・遷移先・ロールバック等）を把握する
   - 画面名称が渡されていない場合はソース（テンプレート名・画面タイトル等）から読み取り、試験項目票ディレクトリ内の `試験項目票_{画面名称}_*.md`（分類を問わない）と照合して画面を特定する。一致するファイルが1つも無い、または複数画面分の候補があり一意に特定できない場合は、実行状態を「失敗」とし確認したい内容（候補ファイル）を添えて返す（担当ファイルの有無はこの時点では判定せず、3で判定する）
   - 参照すべき関連ソース（例外ハンドラ・外部サービスクライアント等）が特定できない場合は、憶測で補わず、確認したい内容を添えて該当観点を「未確認」として記録する
3. `試験項目票_{画面名称}_03_異常系.md` を読み込み、各行（No./シナリオ/正常異常/前提条件/操作手順/期待結果）と、表の下のDB確認用クエリ（対応No./確認用クエリ）を把握する
   - ファイルが存在しない場合は、実行状態を「未実施」として大元skillに返す（単体実行の場合はチャットで報告する）。エラーとはしない
   - 「該当する試験項目なし」と記載されたファイルの場合は、ソース側に外部リソースへのアクセス処理が存在するかを確認し、存在すれば「抜け」として指摘する
4. ソースコードと突き合わせ、以下の観点で指摘を洗い出す
   - **抜け**: ソース上の外部リソースアクセス処理ごとに、DBエラー（接続断・制約違反・タイムアウト等。制約はDBスキーマ・エンティティ定義から判断）、ファイル操作エラー（読み書き失敗・権限エラー等）、AWSサービスアクセスエラー（S3・SQS等への接続失敗・権限エラー等）、その他外部リソース起因の異常に対応する行が1件も無いもの
   - **抜け**: 期待結果欄でDB検証が必要と記述されているのに、DB確認用クエリセクションに対応するNo.の行が無いもの（エラー時にデータが不整合な状態で残っていないことの確認等）
   - **誤り**: 前提条件に、エラーを発生させるための外部リソースの状態（「DB接続を切断した状態」「S3バケットへのアクセス権限が無い状態」等）が具体的に記載されていないもの
   - **誤り**: 操作手順の最後に、外部リソースを正常な状態へ復旧させる手順（「DB接続を復旧する」等）が含まれていないもの
   - **誤り**: 期待結果に画面に表示されるエラーメッセージ・遷移先・システムの挙動が具体的に記載されていない、または例外ハンドラ・エラーページテンプレート・メッセージ定義・トランザクション設定と一致しないもの
   - **誤り**: 操作手順・期待結果で画面項目を指す記述に、`id` 属性やclass名などのソースコード上の物理名がそのまま使われているもの（表示ラベル・プレースホルダ・ボタン文言等の論理名で記述されているべき）。一覧行など対象が複数存在しうる箇所で、対象を一意に特定できる表現になっていないもの
   - **誤り**: No.がこのファイル内で1から始まる連番（ゼロ埋め不要）になっていない、または重複しているもの。他の分類別ファイルとの同一No.は重複として扱わない
   - **誤り**: 前提条件と操作手順・期待結果の間の矛盾
   - **誤り**: 外部リソースを意図的に異常状態にする項目が最後にまとめて配置されていない、または各項目内で復旧まで完結しておらず前後の項目に影響を与える並びになっているもの
   - **誤り**: DB確認用クエリの対応No.が試験項目票に実在しないもの、またはクエリの内容が当該行の期待結果の記述と整合していないもの（DBスキーマを参照せずに判定できる観点）
   - **誤り**: DB確認用クエリのテーブル名・カラム名・WHERE条件が対象のDBスキーマと整合していないもの。DBスキーマはpostgres MCPの `list_objects`／`get_object_details` 等で確認する。MCPが利用できない等でスキーマを参照できない場合は、この観点のみを「未確認」とする
   - **分類誤り**: 正常/異常列が「異常」以外の行、または入力値のバリデーション（フロント／サーバ）・正常系など他分類に属する項目が含まれているもの
   - **表記ゆれ**: 同一の画面項目・文言・外部リソース名に対する表記の不統一、複数行を `<br>` で改行する規則からの逸脱等
5. 見つかった指摘事項を重大度順（抜け＞誤り＞分類誤り＞表記ゆれ）に整理する。各指摘は以下の項目を持つ
   - 種別（抜け／誤り／分類誤り／表記ゆれ）
   - 対象No.（新規に追加すべき項目の指摘の場合は「(新規)」と明記）
   - 指摘内容
   - 根拠となるソースコードの箇所（ファイルパス・行、例外ハンドラ・メッセージ文言等）。確認済み回答だけを根拠とする指摘は、その回答の該当内容を根拠とする
   - 指摘・根拠に秘密情報（接続文字列・認証情報等）が含まれる場合は値をマスクする
6. 集約実行の場合は、指摘一覧（種別・対象No.・シナリオ・指摘内容・根拠）・実行状態（成功）・未確認観点（確認したい内容を添える）を大元skillに返す。この子skill自体はHTMLファイルを作成しない
7. 単体実行の場合
   - 指摘が1件以上あれば、試験項目票ディレクトリに `試験項目票レビュー結果_{画面名称}_03_異常系_ソース_{実行日時yyyyMMddHHmm}.html` を新規作成し、種別｜対象No.｜指摘内容｜根拠の4列表として出力する（既存の試験結果票・試験項目票レビュー結果HTMLと同様のスタイル。指摘内容・根拠に含まれるコード等はHTMLエスケープする）。未確認観点があればHTML内に明記する。既に同名ファイルが存在する場合は上書きせず、末尾に `_2`, `_3` … を付与した名前で保存する
   - 指摘が0件ならファイルを作成せず、確認した観点と未確認観点をチャットで報告する
   - 最後に分類・実行状態（成功／未実施／失敗）・指摘件数・未確認観点と、HTMLを作成した場合はその保存先パスを報告する。未確認観点が残る場合は「レビュー未完了」と明示する
8. 処理中にエラーが発生し完了できない場合は、実行状態を「失敗」とし、失敗理由（秘密情報はマスクする）とともに大元skillに返す（単体実行の場合はチャットで報告する）
9. 試験項目票ファイル・ソースコードは変更しない
